<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tuantase172217_PETest2"
        tools:targetApi="31">
        <activity
            android:name=".UpdateStudentActivity"
            android:label="Updating Student"
            android:parentActivityName=".StudentActivity"
            android:exported="false" />
        <activity
            android:name=".AddStudentActivity"
            android:exported="false"
            android:label="Adding Student"
            android:parentActivityName=".StudentActivity" />
        <activity
            android:name=".MajorActivity"
            android:exported="false"
            android:label="Major List"
            android:parentActivityName=".StudentActivity" />
        <activity
            android:name=".StudentActivity"
            android:exported="true"
            android:label="Student List">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>